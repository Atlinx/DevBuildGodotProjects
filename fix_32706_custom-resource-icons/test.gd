extends Node

@export var stringArray = Array()

# Called when the node enters the scene tree for the first time.
func _ready():
#	for i in 50:
#		var path = "test/path"
#		var path_breakdown = path.split("/")
#		path_breakdown = path_breakdown.reverse()		
#		stringArray.append("->".join(path_breakdown))
	print(stringArray)
